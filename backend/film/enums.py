from enum import Enum

class CareerEnum(Enum):
    """Career/Role options for people in content"""
    ACTOR = 'Actor'
    DIRECTOR = 'Director' 
    PRODUCER = 'Producer'
    WRITER = 'Writer'
    VOICE_ACTOR = 'Voice Actor'